port: "${APP_PORT|80}"

data:
  database:
    user: "${DB_USER}"
    pass: "${DB_PASS}"
    name: "${DB_NAME|wave_action_blog}"
    host: "${DB_HOST}"
    port: "${DB_PORT|5432}"

  cache:
    host: "${CACHE_HOST}"
    pass: "${CACHE_PASS}"
    port: "${CACHE_PORT|6379}"
